{
  "name": "tts-kokoro-processor-mcp",
  "version": "1.0.0",
  "description": "MCP Server for TTS Kokoro Processor",
  "type": "module",
  "main": "mcp_server.js",
  "scripts": {
    "start:dashboard": "docker rm -f tts-dashboard-instance 2>nul || true && docker compose -f dashboard-docker-compose.yml up -d"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1",
    "zod": "^3.23.8",
    "uuid": "^10.0.0",
    "express": "^4.18.2",
    "ws": "^8.16.0",
    "cors": "^2.8.5"
  }
}
